# RobotLocationInference

Infer a location of a robot on a grid based on noisy observations using the forward-backward algorithm.
Then run Viterbi algorithm to find the estimated hidden states of the robot
